name: ci

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: python -m pip install -U pip
      - run: python -m pip install -e '.[test]'
      - run: pytest -q
      - run: python -m frequency_operator_composition.experiment --output /tmp/v0.json
      - run: python -m frequency_operator_composition.cable_experiment --output /tmp/v1.json
      - run: python -m frequency_operator_composition.geometry_vs_kinetics --output /tmp/v2.json
      - run: python -m frequency_operator_composition.state_space_attacker --output /tmp/v3.json
